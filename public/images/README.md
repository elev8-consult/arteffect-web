# Image Placement

Replace placeholder assets with real visuals:

- `batches/` -> batch hero images
- `designs/` -> design artworks and galleries
- `artists/` -> artist portraits
- `ngos/` -> NGO logos/featured images
- `products/` -> product mockups

If Sanity image fields are provided, frontend uses those first.
